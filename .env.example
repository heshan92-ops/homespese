# SpeseCasa Environment Variables Configuration
# Copia questo file in .env e modifica i valori

# JWT Secret Key - IMPORTANTE: Cambia questo valore in produzione!
# Genera un secret casuale con: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your_secret_key_here_change_in_production

# Database Path (opzionale, default: ./data/spesecasa.db)
# DB_PATH=/custom/path/to/database.db

# CORS Origins (opzionale, default: *)
# ALLOWED_ORIGINS=http://localhost,https://yourdomain.com
